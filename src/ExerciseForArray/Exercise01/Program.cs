// Write a program that creates an array of integers with the values 1, 2, 3, 4, and 5, and then outputs each value in the array to the console.
int[] Array = new int[] { 1,2,3,4,5};

foreach (int i in Array)
{
    Console.Write( i);
}
